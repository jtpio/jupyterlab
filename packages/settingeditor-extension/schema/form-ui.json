{
  "title": "Settings Editor Form UI",
  "description": "Settings editor form ui settings.",
  "properties": {
    "settingEditorType": {
      "title": "Type of editor for the setting.",
      "description": "Set the type of editor to use while editing your settings.",
      "enum": ["json", "ui"],
      "default": "ui"
    },
    "pluginsToSkip": {
      "title": "Plugins to skip",
      "description": "List of plugin IDs to skip in the settings editor.",
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": [
        "@jupyterlab/application-extension:context-menu",
        "@jupyterlab/mainmenu-extension:plugin"
      ]
    }
  },
  "additionalProperties": false,
  "type": "object"
}
